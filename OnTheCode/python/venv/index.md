# Venv

在当前文件夹下建立一个虚拟环境，把包（依赖）都放在里面，不会影响到外面的环境。



建立一个新的环境，放在当前的`env`文件夹里的：

```bash
python3 -m venv env
```

用的时候activate一下

```bash
source env/bin/activate
```

不用的时候deactiviate

```bash
deactivate
```







reference:

[1] 

